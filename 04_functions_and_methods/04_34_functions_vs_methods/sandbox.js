const name = "Silviu";

// functions
const greet = () => "Hello";
let resultOne = greet();
console.log(resultOne);

// methods
// methods are invoked using . notation
// methods are still functions, but are functions that are associated with an object or data type
let resultTwo = name.toUpperCase();
console.log(resultTwo);